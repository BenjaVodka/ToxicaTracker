# ToxicTracker Frontend Environment Variables
# Copy this file to .env and update the values as needed.

# Base URL of the Spring Boot backend (without /api suffix)
# Local dev with Vite proxy can use empty value.
VITE_API_BASE_URL=http://localhost:8081
